@model IEnumerable<DashBoard.Models.User>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
        <table class="table">  <tr>
  <th>
             @*       @Html.DisplayNameFor(model => model.Firstname)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Lastname)
                </th> <th>
                    @Html.DisplayNameFor(model => model. Email)*@
  </th>
  <th>
                    @Html.DisplayNameFor(model => model.Password )
                </th>
                <th></th>
        </tr>

       @* @foreach (var item in Model)
        {
            using (Html.BeginForm("Delete", "Home", new { id = item.id }, FormMethod.Post)){

<tr>
  <td>
                        @Html.DisplayFor(modelItem => item.Firstname )
  </td>
  <td>
                        @Html.DisplayFor(modelItem =>  item.Lastname)
  </td>
  <td>
                    @Html.DisplayFor(modelItem =>  item.Email)
  </td>
  <td>
                    @Html.DisplayFor(modelItem =>  item.Password)
  </td>
  <td>
                        <input type="submit" value="Delete" name="Basbuna" />

                    </td>
                   
                </tr>
            }
            using (Html.BeginForm("Add", "Home", new { id = item.id }, FormMethod.Post))
            {
                <td>
                    <input type="submit" value="Add" name="AddBu" />
                </td>
            }
        }*@
</div>
